#pragma once

namespace fl::primitives {

// The four planned progression cycles. Content membership does not gate encounters.
enum class Cycle { Origin, Resonance, Conflict, Singularity };

} // namespace fl::primitives
